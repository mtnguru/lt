//import React, {useState, useEffect} from 'react';

const MsgItem = (props) => {

  return (
    <div className='msg-item'>
      <div>className='payload'>{props.msg}</div>
    </div>
  )
}

export default MsgItem
