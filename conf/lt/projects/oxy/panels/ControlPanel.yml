name: Experiment Control Panel
projectId: oxy
regions:
# header:
#   title: Cabin Controls
  top:
    cmetrics:
      - metricId: ps_na_human_v
        label: Volts
        actionId: hum
        component: MetricSlider

      - metricId: ps_na_human_a
        label: Amps
        actionId: hum
        component: MetricSlider

      - metricId: ez_na_mtr_c
        label: EZ Temperature
        actionId: hum
        component: MetricSlider

      - metricId: ez_na_smarttrack_slpm
        label: EZ Flow Rate
        actionId: hum
        component: MetricSlider

      - metricId: outside_bunkhouse_k_f
        actionId: inp
        component: MetricFull

      - metricId: outside_frontdoor_ow_f
        actionId: inp
        component: MetricFull

